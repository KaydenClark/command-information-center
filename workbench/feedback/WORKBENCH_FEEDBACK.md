# Command Information Center - Harness Feedback

> Generated from LLM Workbench v2.3.

This append-only log carries reusable control-surface friction back to the LLM
Workbench. Product bugs and feature work belong in stable project specs.

| Date | Doc / section | What happened | Impact | Proposed change | Status |
|---|---|---|---|---|---|
| 2026-07-28 | Root docs / `.git/info/exclude` | A currency-refresh task assumed a small set of project-specific docs present on disk in the primary checkout were ordinary tracked docs to cross-link from the harness controls. They are excluded via local `.git/info/exclude` ("kept out of the public repo") rather than tracked or `.gitignore`d — so `git ls-files`/a fresh clone/this task's own worktree (built from `origin/Integration`) never see them, and nothing in the tracked tree documented that this local-only tier existed or why. An agent reasoning from the pushed repo alone had no way to discover these files or avoid linking to them (which would produce dead links for every other clone). *(This row was redacted after the fact — see the next row — because the original wording named the excluded filenames in this tracked, publicly-pushed file, which is itself the disclosure this feedback is about.)* | Any agent asked to "reference project-specific docs" from outside this exact machine/checkout will either miss this tier entirely or, worse, add broken links to files no one else can see. The public/private split relies on tribal knowledge of one local exclude file. | Document the exclusion mechanism generically (it exists, where it's declared, that clones/worktrees never see it) without enumerating the excluded filenames or their purposes in any tracked file. | landed |
| 2026-07-28 | `RUNBOOK.md` (public repo) | SECURITY INCIDENT, self-caught and fixed same session: a follow-up task instructed to "document the local-only tier" took it literally and committed+pushed the exact filenames and one-line purpose of every excluded doc into this public repo's `RUNBOOK.md` (commit `0f3c2c8`) — defeating the confidentiality `.git/info/exclude` was providing. No secrets/credentials were exposed, only internal planning-doc filenames and topics, but that is still information the exclusion existed specifically to keep out of the public repo. Caught immediately (flagged by the harness's own security review), reverted (`fc08f97`), and replaced with a generic note (`d2313fc`) that keeps the useful discovery hint without naming anything. | A public repo briefly carried the exact list of what it was hiding and why, on a pushed (not yet merged) branch. | General Workbench principle: when a doc that lives on the *disclosing* side of a confidentiality boundary explains why something is excluded, it must describe the mechanism only, never enumerate the hidden contents or their purposes. Worth stating explicitly in the canonical harness template so this class of mistake doesn't recur in other projects. | landed |
| 2026-09-04 | `skills/adoption/SKILL.md` opening rule and `tools/workbench-adoption.mjs` | CIC was already adopted at v2.3 (recorded in S-002), so by the adoption skill's own rule -- "Use Adoption only when an existing project is joining the Workbench for the first time... do not rerun Adoption to update an already-adopted project" -- Adoption was the forbidden route. But `workbench-adoption.mjs migrate` is the only seam that builds a v3 support root from a v2 root layout; `workbench-layout.mjs migrate` handles manifest schema 1 to 2 only and needs a manifest CIC never had. The one tool able to do the job was gated behind a skill forbidding the case, so the run used it anyway. Full evidence trace in this lane at `REPORT-v3-1-1-adoption-2026-09-05.md`, finding F-001. | `workbench/manifest.json` now records `provenance.lifecycle: "adoption"` for a room that was upgrading. A future agent reading it will conclude CIC joined the harness in September 2026, contradicting S-002. Every later room making the same move will misrecord its own history the same way. | Name the v2-root to v3-support-root transition as its own lifecycle, or explicitly permit Adoption for it, and make the manifest's accepted `provenance.lifecycle` values match the decision. | open |
| 2026-09-04 | `skills/update-harness/SKILL.md` steps 1.3, 3, and 4 | The skill that should have governed this work names its canonical source as `$INSTANCE_ROOT/Foundry/Halls/Forge`; on this host `INSTANCE_ROOT` is unset and no `Foundry` directory exists. Its later steps instruct creating `specs/S-###-slug/SPEC.md`, copying `tools/spec-workbench.mjs` exactly, and running `node tools/spec-workbench.mjs render` -- all v2 root-layout paths superseded by the manifest-declared lanes. Finding F-002 in the same report. | An agent that correctly selects `/update-harness` for an already-adopted project fails at its first verification step, and if it proceeds is steered to write v2 paths and a root-level tool copy into a v3 room. Together with the row above, the correct route is unusable and the usable route is forbidden. | Rewrite `update-harness` for v3.1.1: a resolvable source location, manifest lanes, `workbench/tools/`, receipt verification, and an explicit name for the layout-generation case. | open |
| 2026-09-04 | `workbench/tools/workbench-layout.mjs` line 213 and the `tools/workbench-adoption.mjs` usage string | `workbench/tools/.workbench-tools.json` records the exact source commit `fa04e27261497ad5aa2f62085764fb6581b2e7e1`, but `workbench/manifest.json` records `"commit": "unrecorded"`. The layout tool resolves `options['--source-commit'] ?? 'unrecorded'`, and the adoption migrate command accepts only `--project`, `--home`, and `--version`, so the migrate path cannot populate the field. The implementing agent recorded the commit in both places it could, the receipt and S-031. Finding F-003. | The manifest is the declared single support-path authority, so its provenance block reads as authoritative while being empty; two records of one fact, one blank, is a drift generator. It also blocks mechanical cross-room reporting on which harness commit each room actually accepted. | Thread the resolved source commit from the adoption run into `manifest.provenance.source.commit`, or remove the field so the receipt is the single record. Add red/green coverage for whichever is chosen. | open |
| 2026-09-04 | `templates/ADOPTION.md` section "What A Finished Adoption Must Prove" | Before these rows, this log's newest entry was 2026-07-28, even though the 2026-09-04 adoption produced at least the three frictions above. No completion checklist in `ADOPTION.md` or in the adoption skill asks whether a feedback row is owed; ADOPTION.md's only feedback instruction is to copy `REPORT_FORMAT.md` into the lane when missing, which is a setup step rather than a harvest step. Finding F-004. | Friction that was known during a run stays recoverable afterwards only by artifact archaeology. These four rows were reconstructed that way, five weeks after the lane was installed and never asked for. | Add one line to the completion checklist: harness friction observed during the run is appended to the feedback lane, or `none observed` is recorded with a reason. Ask the same question in `GENESIS.md` and on the upgrade path. | open |
